# TODO - Reemplazar axios por fetch

- [x] Actualizar `app.js` para usar `fetch` en lugar de `axios`.
- [x] Actualizar `package.json` para quitar `axios` y agregar `node-fetch`.

- [x] Ejecutar `npm install`.
- [ ] Probar `npm start` y validar que la vista muestra los productos.

